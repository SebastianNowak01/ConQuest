package com.example.conquest.data.classes

enum class CosplayStatusFilter(val label: String) {
    All("All cosplays"),
    Planned("Only planned cosplays"),
    InProcess("Only cosplays in process"),
    Completed("Only completed cosplays"),
}

